import React from 'react'
import { View, Text } from 'react-native'

export default function CurrentLocation() {
  return (
    // clickable address bar that shows current location ? should it be a button
    <View>
      <Text>Address</Text>
    </View>
  )
}
